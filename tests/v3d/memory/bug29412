puts "# ==================================================================="
puts "# 0029412: Huge Memory leak since I upgraded to OC 6.9.0"
puts "# ==================================================================="
puts ""

pload VISUALIZATION QAcommands
pload OPENGL
vcaps -vsync 0
vinit View1
vglinfo

OCC29412 100 ;# just to initialize

meminfo
vmemgpu

set listmem {}
set aNbChecks 50
set aMemBase [meminfo h]
for {set anIter 1} {$anIter <= $aNbChecks} {incr anIter} {
  OCC29412 200
  set aMemCurr [meminfo h]
  lappend listmem $aMemCurr
}
puts $listmem

checktrend $listmem 0 1 "Memory leak detected"

meminfo
vmemgpu 
